
services:
  iptv-streamer:
    build: .
    container_name: iptv-streamer
    ports:
      - "8928:8000"
    volumes:
      - /srv/dev-disk-by-uuid-90b0a4e51-9d7c8888888888data/dockers/appdata/iptv-streamer/data:/app/data          # Persistent channel config
      - /srv/dev-disk-by-uuid-90b0a4e51-9d7c8888888888data/dockers/appdata/iptv-streamer/logs:/app/logs 
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - iptv-net

networks:
  iptv-net:
    driver: bridge
